<Window x:Class="CookMaster.Views.ForgotPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CookMaster.ViewModels"
        xmlns:local="clr-namespace:CookMaster"
        mc:Ignorable="d"
        Title="Glömt Lösenord" Height="768" Width="1024" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:ForgotPasswordViewModel />
    </Window.DataContext>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Glömt lösenord?" FontSize="20" FontWeight="Bold"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,12,0,0">
            <TextBlock Text="Användarnamn"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" MinWidth="260"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{Binding Message}" Foreground="Tomato" Margin="0,8,0,0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,14,0,0" HorizontalAlignment="Center">
            <Button Content="Submit" Command="{Binding SubmitCommand}" Padding="12,6" Margin="8,0,0,0"/>
            <Button Content="Back" Command="{Binding BackCommand}" Padding="12,6" Margin="8,0,0,0"/>
        </StackPanel>

    </Grid>
</Window>
