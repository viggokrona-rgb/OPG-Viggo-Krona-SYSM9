<Window x:Class="CookMaster.Views.TwoFactorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CookMaster.ViewModels"
        xmlns:local="clr-namespace:CookMaster.Views"
        mc:Ignorable="d"
        Title="2FA" Height="300" Width="420" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:TwoFactorViewModel />
    </Window.DataContext>
    <Grid Margin="16">




        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="2FA" FontSize="18" FontWeight="Bold" />

        <StackPanel Grid.Row="1" Margin="0,12,0,0">
            <TextBlock Text="A 6-digit verification code has been sent to your email (simulated)." TextWrapping="Wrap" />
            <TextBlock Text="Enter code:" Margin="0,8,0,0" />
            <TextBox Text="{Binding Code}" MaxLength="6" Width="160" />
            <TextBlock Text="{Binding Message}" Foreground="Tomato" Margin="0,8,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,12,0,0">
            <Button Content="Verify" Command="{Binding VerifyCommand}" Padding="12,6" />
            <Button Content="Resend" Command="{Binding ResendCommand}" Padding="12,6" Margin="8,0,0,0" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" Padding="12,6" Margin="8,0,0,0" />
        </StackPanel>

    </Grid>
</Window>
