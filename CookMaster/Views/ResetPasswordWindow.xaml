<Window x:Class="CookMaster.Views.ResetPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CookMaster.ViewModels"
        xmlns:local="clr-namespace:CookMaster"
        mc:Ignorable="d"
        Title="Återställ lösenord" Height="420" Width="600" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:ResetPasswordViewModel />
    </Window.DataContext>
    <Grid Margin="16">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Återställ ditt lösenord" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

      

       

        <StackPanel Grid.Row="1" Margin="0,12,0,0" Visibility="{Binding IsSecurityStepVisible}">
            <TextBlock Text="Security question:" FontWeight="SemiBold" />
            <TextBlock Text="{Binding SecurityQuestion}" Margin="0,6,0,0" />
            <TextBlock Text="Answer" Margin="0,8,0,0" />
            <TextBox Text="{Binding SecurityAnswer, UpdateSourceTrigger=PropertyChanged}" Width="360" />
            <Button Content="Validate" Command="{Binding ValidateAnswerCommand}" Padding="8,4" Margin="0,8,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,12,0,0" Visibility="{Binding IsResetStepVisible}">
            <TextBlock Text="New Password" />
            <PasswordBox local:PasswordHelper.BindPassword="True" local:PasswordHelper.BoundPassword="{Binding NewPassword, Mode=TwoWay}" Width="260" />
            <TextBlock Text="Confirm New Password" Margin="0,8,0,0" />
            <PasswordBox local:PasswordHelper.BindPassword="True" local:PasswordHelper.BoundPassword="{Binding ConfirmNewPassword, Mode=TwoWay}" Width="260" />
            <Button Content="Reset Password" Command="{Binding ResetPasswordCommand}" Padding="8,4" Margin="0,8,0,0" />
        </StackPanel>

        <TextBlock Grid.Row="4" Text="{Binding Message}" Foreground="Tomato" Margin="0,12,0,0" />


    </Grid>
</Window>
