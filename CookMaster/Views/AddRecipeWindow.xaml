<Window x:Class="CookMaster.Views.AddRecipeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views"
        xmlns:vm="clr-namespace:CookMaster.ViewModels"
        mc:Ignorable="d"
        Title="Lägg till recept" Height="768" Width="1024" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:AddRecipeViewModel />
    </Window.DataContext>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Text="Lägg till nytt recept" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="100,0,100,0">
          
            <TextBlock Text="Receptnamn" FontWeight="Bold" Margin="4"/>
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Height="50"/>
           
            <TextBlock Text="Ingredienser" FontWeight="Bold" Margin="0,8,0,0"/>
            <TextBox Text="{Binding Ingredients}" AcceptsReturn="True" Height="50"/>

            <TextBlock Text="Instruktioner" FontWeight="Bold" Margin="0,8,0,0"/>
            <TextBox Text="{Binding Instructions}" AcceptsReturn="True" Height="50"/>

            <TextBlock Text="Kategori" Margin="0,12,0,0" FontWeight="Bold"/>
            <TextBox Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsReadOnly}"/>

            <TextBlock Text="Tid (Minuter)" Margin="0,12,0,0" FontWeight="Bold"/>
            <TextBox Text="{Binding Time, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsReadOnly}"/>

        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">

            <Button Content="Spara" Width="100" Margin="4" Command="{Binding SaveCommand}"/>
           
            <Button Content="Avbryt" Width="100" Margin="4" Command="{Binding CancelCommand}"/>
            
        </StackPanel>
        



    </Grid>
</Window>
