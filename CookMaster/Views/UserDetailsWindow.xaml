<Window x:Class="CookMaster.Views.UserDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CookMaster.ViewModels"
        xmlns:local="clr-namespace:CookMaster"
        mc:Ignorable="d"
        Title="UserDetailsWindow"  Height="480" Width="480" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:UserDetailsViewModel/>
    </Window.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="User Details" FontSize="20" FontWeight="Bold"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,12,0,0">
            <TextBlock Text="Current Username" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding CurrentUsername}"/>

            <TextBlock Text="Current Country" FontWeight="SemiBold" Margin="0,6,0,0"/>
            <TextBlock Text="{Binding CurrentCountry}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,12,0,0">
            <TextBlock Text="New Username"/>
            <TextBox Text="{Binding NewUsername, UpdateSourceTrigger=PropertyChanged}" MinWidth="240"/>

            <TextBlock Text="New Password" Margin="0,8,0,0"/>
            <PasswordBox local:PasswordHelper.BindPassword="True" local:PasswordHelper.BoundPassword="{Binding NewPassword, Mode=TwoWay}" MinWidth="240"/>

            <TextBlock Text="Confirm Password" Margin="0,8,0,0"/>
            <PasswordBox local:PasswordHelper.BindPassword="True" local:PasswordHelper.BoundPassword="{Binding ConfirmPassword, Mode=TwoWay}" MinWidth="240"/>

            <TextBlock Text="Country" Margin="0,8,0,0"/>
            <ComboBox ItemsSource="{Binding AvailableCountries}" SelectedItem="{Binding SelectedCountry}" MinWidth="240"/>

            <TextBlock Text="{Binding Message}" Foreground="Tomato" Margin="0,8,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,16,0,0" HorizontalAlignment="Left">
            <Button Content="Save" Command="{Binding SaveCommand}" Padding="12,6"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Padding="12,6" Margin="8,0,0,0"/>
        </StackPanel>


    </Grid>
</Window>
